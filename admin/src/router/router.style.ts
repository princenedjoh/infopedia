import styled from "styled-components";

export const Main = styled.div.attrs({
    className : `Main w-full h-screen relative overflow-hidden`
})``

export const Outlet = styled.div.attrs({
    className : `Outlet overflow-auto mt-[50px]
     h-screen w-full pb-[100px] box-border`
})
``